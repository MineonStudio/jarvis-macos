# 会议记录模块审计

审计日期：2026-09-24

范围：Jarvis macOS 会议记录模块的主界面、会议详情、录音/系统音频采集、转写、AI 总结、持久化、搜索、恢复与辅助功能。
目标用户任务：快速开始一场会议录音；确认录到了预期音源；会后快速找到结论、决策和待办；需要时核对原文、回听并导出。

## 总体判断

模块的基础链路完整：本地保存录音和逐字稿、会议总结走用户配置的 AI 服务；历史和详情分开存储；处理进度、失败重试、总结检查点和中断恢复均已有实现。录音、播放、全文详情和 Markdown 导出之间也有基本连通。

当前最需要处理的是“用户能否确认内容完整、能否准确找到记录、能否读懂长内容”。搜索有两类确定性问题；系统音频采集可能在录音开始后失败，但界面只短暂提示；详情页把总结与整场逐字稿串成长页面，缺少定位工具。逐字稿可访问性标签还可能覆盖原文，值得作为优先修复项。

针对总结本身的结论是：**当前生成策略更偏“尽量不漏”，没有把“先给结论、压缩重复、突出下一步”设为明确目标。** 这和用户感觉不够标准、不够高效是吻合的；主要原因来自摘要提示词、分块合并规则和页面排序，不只是视觉样式。

总体健康度：**功能基础较完整，日常检索与长会议阅读体验偏弱；录音来源状态和屏幕阅读器语义有较高风险。**

## 本轮实际检查步骤

| 步骤 | 检查内容 | 健康度 | 证据 |
|---|---|---|---|
| 1 | 进入会议记录，查看历史列表、搜索、工具栏和录音入口 | 良好 | 本轮运行中的主窗口截图与辅助功能树 |
| 2 | 打开已完成会议，检查标题、状态、录音播放器和总结 | 一般 | 本轮运行中的详情首屏截图；内容层级明确，但首屏总结较长 |
| 3 | 向下检查待办、未解决问题、说话人和逐字稿 | 需改进 | 本轮运行中的详情下半部截图；长逐字稿占据连续滚动区域 |
| 4 | 检查搜索无结果、其他会议逐字稿搜索、权限缺失、录音、总结失败恢复等路径 | 部分可核验 | 通过当前代码和既有测试静态检查；没有实际录制、请求麦克风/屏幕权限或触发云端总结 |

截图证据在本次审计会话中实际捕获并检查：一张详情首屏，一张滚动到说话人与逐字稿的下半部。当前截图捕获界面未提供把原图保存到工作区文件夹的能力，所以本报告没有内嵌截图文件。实际界面证据仅覆盖当前已有的一条已完成会议；空状态、下载状态、权限弹窗及错误状态来自代码检查，不视为已实机验证。

## 优点

- **录制前置条件明确。** 首次使用时说明本地模型下载、约占用 650 MB，模型未就绪时禁用开始录制。
- **本地与云端边界有说明。** 空状态写明原始录音和逐字稿留在本机，总结使用设置里的 AI 服务；与 README 的数据流描述一致。
- **会后内容有结构。** 总结分为关键讨论、明确决策、待办和未解决问题；待办显示负责人和截止时间。
- **能从原文回听。** 逐字稿分段行可跳到对应音频时间，音频播放器支持拖动进度。
- **恢复思路扎实。** 应用退出时会把录音/转写/总结中断转换为可重试状态；AI 总结按转写分块保存检查点，避免失败后重复全部请求。
- **存储实现考虑了长记录。** 会议元数据与逐字稿/总结分开保存；录音路径校验文件名；删除前明确说明会同时删除录音和逐字稿且无法恢复。
- **macOS 入口齐全。** 工具栏、菜单栏和可配置快捷键都能开始或停止录音；历史行提供上下文菜单。

## 主要问题与建议

### P1 — 逐字稿的 VoiceOver 标签可能隐藏原文

`MeetingTranscriptSection` 把整行设置成按钮，并用 `.accessibilityLabel("从…开始播放")` 覆盖按钮标签（`Sources/Jarvis/Views/MeetingContentViews.swift:1093-1121`）。VoiceOver 可能只读出时间和播放动作，不读说话人及逐字稿正文，用户无法用屏幕阅读器核对转写内容。

**建议：** 保留正文作为可访问名称，例如“说话人 1，00:36，正文……；双击从此处播放”；播放动作使用提示或独立播放按钮表示。确认 VoiceOver 能读出完整段落，且名称、时间和正文顺序合理。

### P1 — 系统音频失败后，录音中看不到当前采集来源

录音器先启动麦克风，再尝试通过 ScreenCaptureKit 采集系统音频；系统音频启动失败时仍继续录音，只显示短暂的“已开始录音，未采集系统音频”提示（`Sources/Jarvis/MeetingRecorder.swift:58-84`、`Sources/Jarvis/AppModel+Meetings.swift:161-200`、`Sources/Jarvis/JarvisFeedbackCopy.swift:57`）。录制中的详情只显示“原始音频正在本机写入”，不持续说明麦克风/系统音频哪些正在采集（`Sources/Jarvis/Views/MeetingContentViews.swift:783-805`）。用户切到其他页面或错过 Toast 后，可能整场都没录到会议播放音频。

**建议：** 在录音状态区固定显示“麦克风已连接”“系统音频未采集”等逐源状态；系统音频不可用时提供原因和可恢复动作（权限跳转或重新尝试），并在开始后明确告知当前录音只包含麦克风。不要让用户必须猜测捕获是否完整。

### P1 — 搜索无匹配时，右侧详情仍展示第一条会议

搜索文本变化时会把不再匹配的选中项改成首个匹配 ID；若没有结果，ID 变成 `nil`（`Sources/Jarvis/Views/MeetingContentViews.swift:75-81`）。但 `selectedRecord` 遇到 `nil` 或无效 ID 时又回退到全部记录的第一条（同文件 `89-95`）。因此左侧会显示“没有匹配的会议”，右侧却继续展示一条与搜索词无关的会议，结果与详情互相矛盾。

**建议：** 区分“未选择”和“搜索为空”：有查询词且零结果时，详情区显示搜索空状态；只有未搜索且未选择时才显示“选择一条会议记录”。

### P2 — 搜索其他会议的逐字稿不可靠

`matchesSearch` 包含逐字稿文本匹配（`Sources/Jarvis/MeetingModels.swift:334-341`），但仓库将元数据与详情分开保存，列表最初读取的记录不含逐字稿；详情只在选中会议时懒加载（`Sources/Jarvis/MeetingRepository.swift:47-55, 117-122`、`Sources/Jarvis/AppModel+Meetings.swift:293-300`）。因此未选中的会议通常没有可供 `matchesSearch` 搜索的正文。界面称为“搜索会议”，但用户输入正文内容可能找不到对应记录。

**建议：** 为列表维护可搜索的轻量索引（标题、摘要、逐字稿词项），或在搜索时异步查询详情文件；避免为搜索所有会议而把完整逐字稿常驻内存。匹配摘要和待办会更符合用户按结论找会议的习惯。

### P2 — 详情页过长，重点与原始记录竞争注意力

实机截图显示总结之后紧接说话人列表和逐字稿；逐字稿每段都以可点击行展示，长段落在宽度受限的正文列中换行，用户需要滚过大量文本才能浏览完整会议。没有摘要/逐字稿折叠、页内定位、逐字稿搜索或说话人筛选。录音约十分钟的样本也已形成明显长页；更长会议影响会更大。

**建议：** 默认突出总结和待办，逐字稿默认折叠或放到独立标签；加入章节跳转/固定目录、逐字稿内查找和按说话人筛选。保持点击时间戳回听的能力，并在滚动时提供清晰的当前章节提示。

### P2 — AI 结论缺少原文出处与轻量校正

AI 汇总提示要求事实引用逐字稿片段 ID（`Sources/Jarvis/MeetingSummaryService.swift:134-154`），抽取事实也会保留来源到检查点；但交给最终纪要合成的 `SynthesisFact` 只保留类别、文字、负责人和截止时间，没有保留来源 ID（同文件 `361-370`）。最终 `MeetingSummary` / `MeetingActionItem` 也没有出处字段。页面因此无法展示来源或把结论/待办跳回相关逐字稿。用户无法快速判断“这是谁承诺的”“这个决定是否有原文支持”。待办显示负责人和期限，但没有标记完成或编辑的能力。

**建议：** 每条决策和待办提供“查看原文”入口，跳转到对应时间并播放；允许用户修正文案、负责人和期限。完成状态可作为可选的本机元数据保存，不要将 AI 推断直接视为已确认事实。

## 对“总结标准与效率”的专项审计

### 为什么当前总结容易显得冗长

1. **抽取阶段要求“保留全部明确事实”。** `requestFacts` 让模型把明确事实都提出来，再去重，但没有要求优先级、重要性、每类条数或字数上限（`Sources/Jarvis/MeetingSummaryService.swift:134-140`）。这是事实提取的合理做法，却不应直接决定最终用户看到的摘要长度。
2. **合成阶段明确反对删减。** 摘要和多段合并都要求保留“所有相关事实/所有不重复的有效信息”，并要求不要因为篇幅主动删除（同文件 `209-215, 251-256`）。缺少精简目标时，模型更倾向把讨论内容逐条保留，产生重复、次要细节占位和“关键讨论”列表过长。
3. **实际去重偏严格。** 事实只按类别、文字、负责人、期限完全相同才合并（同文件 `343-358`）；摘要列表只删除完全相同的字符串（`496-503`）。意思相近但措辞不同的条目会保留。分块总结在最后再合并，重复和分类漂移的机会更多。
4. **概览没有强制长度上限。** 提示词说“一句话结论”，但没有字符/句子限制，也没有程序侧校验。实际会议详情的概览在一段中塞入多个议题和判断；“关键讨论”又进一步列出大量转写细节，难以快速扫读。
5. **分类少了标准会议纪要里常用的上下文。** 当前结构为 overview、关键讨论、明确决策、待办、未解决问题；没有显式的会议目的/议题、结论优先级、风险/阻塞项、参与人确认、行动项状态或来源。转写器识别的“说话人 1/2/3”并不等于已确认参与者。

### 页面如何增加阅读成本

- 页面顺序是概览 → 关键讨论 → 决策 → 待办 → 未解决问题（`Sources/Jarvis/Views/MeetingContentViews.swift:991-1005`）。关键讨论可能很长，导致最有行动价值的待办被推到后面。
- 每一类内容都放在一张卡内，列表条目使用相同的圆点和正文样式，没有摘要/详细程度切换、优先级标记、快速导航或折叠。重点只能靠滚动位置和文字本身识别。
- 待办只是任务文字加负责人/截止时间，没有状态、完成操作、编辑或回原文按钮（同文件 `1031-1054`）。用户需要把会议信息再次手工整理进自己的任务系统。
- “复制纪要”和“导出 Markdown”调用同一个 `markdownDocument()`，这个文档除了总结还总是附上完整逐字稿（`Sources/Jarvis/AppModel+Meetings.swift:302-333`、`Sources/Jarvis/MeetingModels.swift:414-460`）。用户想快速复制一份可转发的短纪要时，无法选择“只要总结”；反过来，想要完整记录也没有明确的导出选项说明。

### 更合适的默认输出结构

建议把“高效可执行”设为默认，把完整内容保留为二级展开。每场会优先展示：

1. **会议结论：** 一到两句话，说清目标、结论和是否达成。
2. **决策：** 只列明确决定；如有分歧，注明未达成一致。
3. **待办：** 任务、单一负责人、截止时间、状态、原文时间点；缺失信息明确显示“未指定”，不推断补齐。
4. **风险与待确认：** 卡点、依赖和开放问题；只列会影响执行的内容。
5. **讨论摘要：** 按议题分组，每个议题仅保留必要背景和关键观点，默认折叠。
6. **逐字稿：** 作为可搜索、可筛选、可点时间回听的原始依据，和摘要分开浏览。

生成策略应在最终整理阶段单独做“重要性排序与精简”，而不是要求事实抽取器丢弃信息。保留完整事实供逐字稿/来源查验；最终摘要按用户可读的条目数和长度目标出稿。长会先按议题合并，再生成简洁全局概览；跨分块合并后增加重复检查与类别校验。对没有足够证据的任务负责人、日期或决策状态返回空值/待确认，不做猜测。

### 交互与导出建议

- 把 **待办和决策移到概览后面**，讨论摘要放在它们后面；会议详情默认落在结论与行动项，而不是需要先读完长摘要才能找到任务。
- 增加“精简 / 详细”查看切换；详细模式提供议题分组、摘要折叠和逐字稿搜索。
- 每条决策/待办提供原文跳转；待办支持编辑、完成和复制单项。
- 将复制拆为“复制精简纪要”和“复制完整记录”；导出时可选择仅纪要或纪要加逐字稿，并显示大致导出内容。
- 提供“重新生成精简版”时保留原总结或可撤销，避免覆盖人工修订；明确重生成会再次调用当前配置的 AI 服务。

### P2 — 固定字号与窄窗口重排需要进一步验证

会议详情大量使用固定点数的 `Font.system(size:)`（`Sources/Jarvis/Views/MeetingContentViews.swift:5-9`），没有看到面向辅助功能文字大小的缩放策略。截图字号看起来清晰，但放大系统文字、窄窗口或 VoiceOver 键盘导航尚未验证；逐字稿的长文本和多字段横排是最容易出现重排或阅读负担的位置。

**建议：** 将正文样式映射到可缩放的语义文本样式，关键操作至少 44 pt 左右可命中；在辅助功能大字和较窄窗口下检查播放控件、负责人/期限、标题和逐字稿换行。颜色需和标签/形状共同表达说话人，不能只靠颜色区分。

### P3 — 语言枚举有英文，但用户侧目前只有中文入口

`MeetingLanguage` 声明了中文与 English（`Sources/Jarvis/MeetingModels.swift:3-22`），录音入口却固定走 `.simplifiedChinese`，模型准备页也只有中文转写模型（`Sources/Jarvis/AppModel+Meetings.swift:133`、`Sources/Jarvis/Views/SettingsContentView.swift:620-646`）。如果 English 是计划能力，目前没有可见设置或状态提示；如果暂未支持，模型中的枚举会让实现能力边界显得不清楚。

**建议：** 明确当前支持范围。近期仅支持中文就移除不可达的英语分支或标为规划；要支持英语时，再提供录音语言选择、相应模型准备状态和录制前提示。

## 代码处理逻辑评估

**整体做得较好的部分：**

- 状态流覆盖 recording → transcribing → transcribed/summarizing → ready/failed/summaryFailed，并有不同失败提示与可重试入口。
- 总结服务采用分块抽取事实再合并，并校验 source segment IDs；长会能够分块处理。检查点与逐字稿 fingerprint、pipeline version 绑定。
- 双轨采集将麦克风和系统音频分别保存，混音时记录系统音频启动偏移；系统音轨失败时保留麦克风录音。
- 仓库迁移旧索引、分文件存元数据和详情，并在索引损坏时禁写，降低覆盖现有数据的风险。
- 删除录音时会一并清理混音、麦克风、系统音频及压缩音轨；文件名校验阻止路径穿越。

**需要从逻辑层继续改进的部分：**

1. 搜索与懒加载的数据契约没有对齐：详情分离后，`matchesSearch` 仍假设记录对象含有全文。需要搜索索引设计，而不是只调整文本过滤。
2. `selectedRecord` 把失效选择回退到首条记录，掩盖了真实的空选择状态，也直接导致无结果时详情错配。
3. 音源采集是部分成功策略，但 UI 未把“部分成功”作为持久状态建模；目前用户反馈依赖瞬时 Toast。建议将采集源及失败原因存进录音会话状态/记录元数据。
4. 当前业务结果以完整段落字符串为主。虽通过结构化 JSON 和分块校验减少格式错误，但 UI 不展示来源引用；人工审阅和更正缺少数据操作接口。
5. 存储提示只在使用量达到 2 GB 时 Toast 警告；未看到会议录音保留、按体积清理或快速查看占用明细入口。长期开启录音会累积文件。建议在设置里给出会议录音用量和明确清理策略；删除动作保持现有二次确认。

## 建议的修复顺序

1. **先修数据获取与辅助功能**：保证读屏能听到逐字稿；零结果时右侧空状态正确，搜索覆盖未选中会议内容。
2. **补齐录音来源状态**：录音开始时和录制期间持续说明麦克风、系统音频状态和权限原因。
3. **改造总结内容策略**：事实完整留档，输出阶段明确压缩重复、结论优先和可执行结构；按议题合并长会并检查跨段重复。
4. **缩短阅读与分享路径**：待办/决策置顶、逐字稿折叠/分区、搜索与说话人筛选、从总结跳原文；提供精简版和完整记录两种复制/导出。
5. **补可校正性与长期管理**：编辑/完成待办、会议存储用量和可恢复或明确不可恢复的删除策略。
6. **界定语言支持**：明确中文限制或补齐英文模型及用户可见选择。

## 证据限制

- 本次观察的是单条现有会议的暗色主题大窗口；没有覆盖浅色主题、不同窗口尺寸、空状态、模型下载、录音中、权限拒绝、错误重试和多会议搜索的运行截图。
- 未通过真实录音、真实 AI 服务或 VoiceOver/键盘操作验证端到端运行行为；相关结论分为“代码可确认”和“需设备/辅助技术复验”。
- 初始审计阶段未运行测试或构建。后续实施验收已运行定向与全量测试、格式检查和 Release 开发版构建，结果见下文。

## 后续实施与验收（2026-09-24）

根据本次审计优先完成了“总结更可执行、出处可核对、搜索可信、分享内容可选”的改进：

- 总结提示词明确限制概览与讨论要点长度，优先结论、决策、待办、风险和未解决事项；分块结果合并时保留逐字稿来源，并校验引用 ID。
- 页面把结论、待办、已确认决策和待确认问题前置，讨论要点默认折叠；待办可标记完成，且会持久化。
- 新总结的决策、待办及讨论项能跳转到对应逐字稿并播放；现有旧总结没有引用时会正常显示，不伪造出处。
- 复制/导出可选精简纪要或含逐字稿的完整记录。历史逐字稿和总结均纳入后台搜索；无匹配时详情显示空状态，不再错配到首条记录。
- 逐字稿 VoiceOver 标签包含说话人、时间和完整正文。
- 对已有总结增加“重新生成”入口，确认后根据当前逐字稿重建；失败时保留已有总结和原始录音/逐字稿。重新生成会调用用户当前配置的 AI 服务。

### 验收结果

| 检查 | 结果 |
|---|---|
| 全量 Swift 测试 | 432 项通过，0 失败 |
| 会议模块定向测试（最后一轮） | 22 项通过，0 失败 |
| SwiftFormat 格式检查 | 6 个改动文件通过 |
| Release 开发版构建 | 成功，使用独立 `com.jarvis.mac.dev` 身份 |
| 实际窗口检查 | 已打开会议记录，确认新页面层级、精简/完整菜单、真实历史数据和逐字稿阅读布局 |

实际窗口也验证了总结改进的必要性：选中的旧会议只显示一段多主题结论和 15 条讨论要点，讨论项内容很长，未能快速呈现该会议可执行的决策/任务。新结构不会自动重写存量总结；需在详情中点击“重新生成”并确认，才会按新规范生成。此次验收没有触发该操作，也没有发送真实 AI 请求或录制音频。

### 尚未覆盖的审计项

- 实际采集麦克风与系统音频的来源状态、权限失败后的恢复操作仍需单独改造和实机验收。
- 未在浅色主题、窄窗口、大字模式、VoiceOver/纯键盘导航下做视觉与辅助技术实测。
- 新摘要质量的端到端验收需要连接用户配置的 AI 服务并重新生成一条代表性会议；本轮通过提示构造和确定性单元测试验证输出约束与引用校验，没有用真实模型重写历史记录。
- 当前逐字稿仍在总结下方连续呈现，缺少逐字稿内搜索、说话人筛选、议题导航；待办可以完成，但编辑负责人/期限还未实现。

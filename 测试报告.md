# 贾维斯（Jarvis）macOS 项目缺陷审计与测试报告

| 项目 | 内容 |
| --- | --- |
| 审计对象 | `MineonStudio/jarvis-macos`（本地工作副本，仓库路径含中文字符） |
| 版本 | v1.3.5（build 332），git HEAD `d0dfe0a` |
| 审计日期 | 2026-09-17 |
| 代码规模 | 112 个 Swift 源文件 / 36,347 行（`Sources/Jarvis`）+ 28 个测试文件 / 6,293 行 |
| 构建方式 | SwiftPM（swift-tools 6.2，Swift 6 语言模式，macOS 26+） |
| 审计方式 | 静态审查 + 依赖注入式复现 + 覆盖率驱动 + 8 路并行子系统深审 + 逐条二次验证 |

---

## 1. 结论摘要

| 指标 | 结果 |
| --- | --- |
| 编译（debug） | ✅ 通过（0 error） |
| 编译（release） | ✅ 通过 |
| 单元测试 | ✅ **291 / 291 通过**（原 283 + 本次新增 8 条回归测试），0 失败，2.0s |
| 代码格式（`swiftformat --lint`） | ✅ 0 / 142 文件需要修改 |
| 静态检查（`swiftlint lint`） | ✅ 0 error（全部为 warning） |
| **行覆盖率** | ⚠️ **21.71 %**（42,181 行中 33,023 行从未执行） |
| **发现缺陷** | **4 严重、9 高、19 中、51 低 = 83 项** |
| **本次可执行复现** | 5 项（含 3 次崩溃 trap 复现、1 次像素级图像复现） |

**核心结论：这个项目"绿得很像样"，但测试全绿是假象。**

291 个测试全部通过，覆盖率却只有 **21.7 %**——**44 个文件、18,269 行（占全部代码 43.3 %）一行都没被执行过**，而这片"零覆盖区"恰好包含了几乎全部业务编排层：

| 零覆盖文件 | 行数 |
| --- | --- |
| `AppModel+Meetings.swift` | 835 |
| `AppModel+Clipboard.swift` | 621 |
| `AppModel+Settings.swift` | 504 |
| `AppModel.swift`（2.8 %） | 831 |
| `MeetingRecorder.swift`（0.9 %） | 432 |
| `ShortcutManager.swift`（16.6 %） | 475 |
| `JarvisWebPlatform.swift`（21.5 %） | 615 |

本次审计发现的 **4 个严重缺陷全部落在这片零覆盖区域**，现有测试在结构上不可能发现它们：

1. 剪贴板历史淘汰会**删除用户磁盘上缓存目录之外的原始文件**；
2. 启动阶段的 `for index in clipboardItems.indices` 循环内缩容导致**启动即崩溃**；
3. 简历"工作年限"输入框输入长数字会触发 `Int(Double)` **致命错误崩溃**；
4. 截图的 mosaic（模糊/像素化）图层**上下镜像**，导出的打码结果与预览不一致。

这四条里有三条我已经用可执行试验坐实（含两次退出码 133 的 SIGTRAP 崩溃）。

---

## 2. 测试环境

| 项 | 值 |
| --- | --- |
| 硬件 | Apple Silicon（arm64） |
| 操作系统 | macOS 27.0（build 26A428） |
| Xcode | 27.0（27A266a） |
| Swift | 6.4（swiftlang-6.4.0.34.1） |
| 依赖 | FluidAudio 0.12.4（`Package.resolved`） |

> **构建环境注意事项**：本机 `swift build` / `swift test` 在受限沙箱下会以
> `sandbox-exec: sandbox_apply: Operation not permitted` 失败——SwiftPM 需要嵌套调用自身的
> `sandbox-exec` 来编译 manifest。加上 `--disable-sandbox` 并把 `HOME` 与模块缓存重定向到工作区后
> 一切正常（`swift build -c release` 同样通过）。这不影响产物正确性，
> 但**意味着 `build_app.sh` 与 CI 在同类受限/沙箱化环境里会直接失败**（见 L-26）。

复现命令：

```bash
export HOME="$PWD/.audit/spm"
swift build --disable-sandbox --scratch-path .build/audit
swift test  --disable-sandbox --scratch-path .build/audit --enable-code-coverage
swiftformat --lint --config .swiftformat Sources Tests
swiftlint lint --config .swiftlint.yml
```

---

## 3. 基线测试结果

### 3.1 单元测试

```
Executed 291 tests, with 0 failures (0 unexpected) in 1.982 seconds
```

291 个测试分布在 29 个文件（原 283 条 + 本次新增 `AuditRegressionTests` 8 条）。
规模最大的几组：`ClipboardTests`(27)、`ResumeTests`(26)、`WallpaperTests`(21)、
`ScreenshotTranslationTests`(20)、`MeetingTests`(19)。

**测试本身写得不敷衍**：没有 `XCTAssertTrue(true)` 之类的空断言，没有 `XCTSkip`，
没有 `XCTExpectFailure` 滥用，每个测试函数至少一条真实断言。问题不在"测得潦草"，而在**测什么**。

### 3.2 覆盖率（`llvm-cov`，已排除 `Tests/` 与 `.build/`）

| 范围 | 行覆盖 |
| --- | --- |
| 全工程 | **21.71 %**（42,181 行 / 33,023 行未覆盖） |
| 0 % 覆盖的文件 | **44 个 / 18,269 行（占全工程 43.3 %）** |

**覆盖率高（纯逻辑层被保护得不错）**

| 文件 | 覆盖率 | 行数 |
| --- | --- | --- |
| `ScreenshotCoordinateSpace.swift` | 100 % | 52 |
| `ScreenshotLanguagePackProbe.swift` | 100 % | 11 |
| `JarvisLogging.swift` | 96.6 % | 408 |
| `ScreenshotTranslationLayout.swift` | 96.2 % | 424 |
| `EntertainmentVideoLink.swift` | 94.8 % | 173 |
| `ScreenshotTranslationOCR.swift` | 94.2 % | 139 |
| `PersistenceSupport.swift` | 90.1 % | 212 |

**覆盖率 0 % 的关键文件（按体量排序，节选）**

| 文件 | 行数 | 备注 |
| --- | --- | --- |
| `Views/MeetingContentViews.swift` | 2,681 | 视图层，可接受 |
| `ScreenshotToolbar.swift` | 1,329 | 布局逻辑未被直接测 |
| `AppModel+Meetings.swift` | 835 | **业务编排，含录音状态机** |
| `ScreenshotCanvasView.swift` | 863 | 标注交互 |
| `JarvisWebPlatformViews.swift` | 787 | WebView 容器视图 |
| `AppModel+Clipboard.swift` | 621 | **含本次两个严重缺陷** |
| `ScreenshotCaptureController+Presentation.swift` | 535 | **含历史编辑画布错配缺陷** |
| `AppModel+Settings.swift` | 504 | **设置持久化** |
| `ScreenshotCaptureController+Pinned.swift` | 410 | 贴图工具栏死代码 |
| `MeetingTranscriptionService.swift` | 232 | ASR 分片 |
| `ThumbnailCache.swift` | 92 | 成本单位错配 |

完整逐文件数据见 `.audit/coverage-report.txt`。

### 3.3 静态检查

- **SwiftFormat**：`0/142 files require formatting` — 通过。
- **SwiftLint**：0 error，全部为 warning。主要类型：`file_length`
  （`Wallpaper.swift` 1479 行、`MeetingContentViews.swift` 1228 行）、
  `type_body_length`（`MeetingSummaryService` 562 行）、`cyclomatic_complexity`
  （`ScreenshotRenderPipeline.swift:40` 复杂度 12）、`force_unwrapping`
  （`JarvisLocalSigning.swift:279`、`ScreenshotTranslationSettingsCards.swift:32`）。
  这些是维护性信号；其中 `JarvisLocalSigning.swift:279` 的 `message!` 经核查受前置
  `isEmpty` 判断保护，**不是**真实缺陷。

### 3.4 本次新增的回归测试

新增 `Tests/JarvisTests/AuditRegressionTests.swift`（8 条用例，全部通过）。设计原则：
缺陷尚未修复时，用 `XCTExpectFailure` 把"当前是坏的"固定下来——这样套件保持全绿，
而一旦有人修好，用例会因"预期失败却没有失败"而报错，提醒删掉该标记。

| 用例 | 锁定内容 |
| --- | --- |
| `testLegacyFileRemovalMustNotDeleteFilesOutsideTheCacheDirectory` | 缺陷 C-1（`XCTExpectFailure`） |
| `testMutatingCollectionWhileIteratingIndicesRequiresReResolution` | 缺陷 C-2 的根因形状 |
| `testMosaicAnnotationIsNotRenderedVerticallyMirrored` | 缺陷 C-3（`XCTExpectFailure`） |
| `testWorkYearsParsingRejectsAbsurdNumericInputInsteadOfTrapping` | 缺陷 C-4 的安全边界 |
| `testPrereleaseTagsAreNotNewerThanTheEquivalentRelease` | 缺陷 H-8 |
| `testSearchGenerationMustOnlyAdvanceWhenALoadActuallyStarts` | 缺陷 H-1 的不变量 |
| `testStaleRevisionIsDiscardedAndNewestWins` | 复核 `JarvisJSONFile` 修订号守卫（防回归） |
| `testExportedSelectionRegionMatchesTheSelectedCanvasRegion` | 复核导出坐标往返（防回归） |

---

## 4. 缺陷清单

> 每条都经过：读完整上下文 → 追调用链确认可达 → 尽量用可执行试验坐实 → 复核是否为近期提交引入的回归。
> `[已复现]` = 本次用可执行试验坐实；`[已确认]` = 逐行读码 + 调用链确认；`[静态分析]` = 受环境限制未能端到端执行。

### 4.1 严重（Critical）

#### C-1 剪贴板历史淘汰会删除用户的原始文件（缓存目录之外）
**`[已复现]`** — `Sources/Jarvis/AppModel+Clipboard.swift:84-89` + `Sources/Jarvis/ClipboardCacheStore.swift:413-416`

```swift
let stalePaths = matchingItems.flatMap(\.cachePaths).filter { !preservedPaths.contains($0) }
    + overflowItems.flatMap(\.cachePaths)
clipboardCacheStore.removeLegacyFiles(atPaths: stalePaths, reason: "historyReplacementOrOverflow")
```

```swift
func removeStoredFile(atPath path: String) {
    ...
    try fileManager.removeItem(atPath: path)   // ← 没有任何"必须位于缓存目录内"的判断
```

**触发链**：`ClipboardItem.cachePaths`（`ClipboardService.swift:252-254`）把
`textPath/imagePath/filePath/thumbnailPath` 全部返回，**不做归属校验**；而
`captureFile`（`ClipboardService.swift:516-523`）在文件 > 1 GB、或缓存已满导致
`storeFile` 返回 nil 时，把**原始文件路径**写进 `filePath` 且 `isStoredCopy = false`：

```swift
let path = storedPath ?? url.path          // 原始文件路径
isStoredCopy: storedPath != nil            // false
```

于是：复制一个 2 GB 视频 → 条目引用用户自己的文件 → 再复制 300 条新内容把它挤出上限
→ **原始视频被删除**。同一文件被复制两次（mtime 变化使 fingerprint 不同）时，
旧条目溢出即可删除新条目仍在引用的文件。

**影响**：缓存目录之外的用户文件被不可逆删除，只留一条 info 级日志。

**回归确认**：`git show a053596` 显示这里**原本**是带归属校验的
`removeManagedFiles(for: removedItems)`，该提交把它换成了无校验的
`removeLegacyFiles(atPaths:)`。仓库自带的
`testClipboardCacheRemovalDoesNotDeleteExternalSourceFile` 只覆盖 `removeManagedFiles` 这一路，
因此没能拦住。

**复现证据**（`AuditRegressionTests`，`XCTExpectFailure` 已接受该失败）

```
SCRATCH external exists before removeLegacyFiles: true
SCRATCH external exists after  removeLegacyFiles: false
SCRATCH VERDICT: DELETED THE USER'S FILE
```

**修复建议**：改回 `removeManagedFiles(for: matchingItems + overflowItems, reason:)`；
`managedFileURLsLocked` 已有 `path.hasPrefix(directoryURL + "/")` **且**
`isStoredCopy || isManagedFilename` 的双重判断。或给 `removeLegacyFiles` 本身补上同样的守卫。

---

#### C-2 启动阶段致命越界崩溃：`migrateClipboardTextCache`
**`[已复现]`** — `Sources/Jarvis/AppModel+Clipboard.swift:9-27`（越界点 `:10`/`:24`）

```swift
for index in clipboardItems.indices {           // ← indices 只求值一次
    let item = clipboardItems[index]            // ← 每轮无条件读下标
    ...
    trimClipboardCacheIfNeeded(forAdditionalBytes: Int64(data.count))   // ← 这个调用会删元素
    clipboardItems[index].textPath = path       // ← 依然用旧下标
}
```

`trimClipboardCacheIfNeeded` 在容量不足时执行 `clipboardItems.removeAll { $0.id == item.id }`
（`:481`）并落盘（`:505`），外层循环持有的 `Range<Int>` 仍是旧长度 → 下一轮越界 **trap**。

**触发条件**：启动时存在至少一条"有 `text` 但没有 `textPath`"的旧格式文本条目
（任何早期版本升级上来都有），且当时缓存里存在**未收藏且有托管文件**的条目、
使迁移过程中的容量淘汰真的删掉了元素。

**影响**：App 在启动后 `applyStartupSnapshot` 阶段直接崩溃退出——用户看到"打开就闪退"。

**复现证据**（最小程序，退出码 133 = SIGTRAP）

```
iteration i=0 count=4
iteration i=1 count=4
  reading a[1] -> 20      ← removeAll 后 count=3
iteration i=2 count=3
Fatal error: Index out of range
EXIT=133
```

**修复建议**：先取 id 快照，再每轮用 `firstIndex(where:)` 重新定位：

```swift
for id in clipboardItems.filter({ ... }).map(\.id) {
    guard let index = clipboardItems.firstIndex(where: { $0.id == id }) else { continue }
    ...
}
```

---

#### C-3 截图 mosaic（模糊/像素化）图层上下镜像，导出内容与预览不一致
**`[已复现]`** — `Sources/Jarvis/ScreenshotRenderPipeline.swift:93-94`（镜像 CTM）+ `:231`（绘制点）

```swift
context.translateBy(x: 0, y: CGFloat(pixelHeight))
context.scaleBy(x: scale, y: -scale)
...
case .mosaic:
    drawMosaic(annotation, in: context, ...)
```

```swift
context.interpolationQuality = annotation.mosaicStyle == .pixelate ? .none : .high
context.draw(filteredImage, in: canvasRect)      // ← 在镜像 CTM 下绘制位图
```

**根因**：这套镜像 CTM 是为**矢量**标注建立的（`device_y = pixelHeight − scale·y`），
矢量标注只把**点**交给映射，所以箭头/矩形是对的。但 `CGContext.draw(image:in:)` 会在
**用户空间**里正立地绘制 CGImage，于是在镜像 CTM 下整幅过滤图被翻转。
底图走的是**非镜像**的 `context.scaleBy(x: scale, y: scale)`（`:71`），两者从此不一致。

**像素级复现**（画布自上而下为 红/绿/蓝/白 四条带，在画布 y=100..140 处放矩形模糊）：

```
SCRATCH source color at same point:   (r 0.017, g 0.198, b 1.000)   ← 蓝色（用户框选的内容）
SCRATCH mosaic center color:          (r 0.000, g 0.977, b 0.000)   ← 绿色
SCRATCH mirrored-position color:      (r 0.000, g 0.977, b 0.000)   ← 与镜像位置一致
SCRATCH VERDICT: MIRRORED
```

导出图中 mosaic 区域呈现的是 **`canvasHeight − y` 处**的画面，而 SwiftUI 画布
（`MosaicAnnotationView`）显示的是用户框选位置的画面。

**影响**：任何带马赛克的截图在**保存 / 复制 / 确认 / 贴图**后，打码块里显示的是镜像位置的
内容。虽然该区域仍被打码（隐私没有直接泄漏），但**用户看到的打码效果与实际产物不是同一块画面**——
对于"确认无误再打码"的使用场景，这是可信度问题：用户无从确认自己遮住的到底是不是那一块。

**修复建议**：在绘制前反向翻转（clip 已经落在设备空间，不受影响）：

```swift
context.translateBy(x: 0, y: canvasRect.height)
context.scaleBy(x: 1, y: -1)
context.draw(filteredImage, in: canvasRect)
```

或把 mosaic 合成挪到与底图相同的非镜像 pass 里。

---

#### C-4 简历"工作年限"长数字输入触发 `Int(Double)` 致命错误崩溃
**`[已复现]`** — `Sources/Jarvis/Resume.swift:206-212`

```swift
guard let range = workYears.range(of: #"\d+(?:\.\d+)?"#, options: .regularExpression),
      let value = Double(workYears[range]),
      value >= 0
else { return nil }
return Int(value.rounded(.down))     // ← 会 trap 的转换
```

`Int.init(Double)` 是**会 trap** 的转换。`Double("9999999999999999999")`（19 位）解析为
1e19 > `Int.max`（9.223e18），直接 abort。

**可达路径**：`ResumeBasicInfo.missingRequiredFieldsForAI`（`Resume.swift:193`）→
`ResumeInspector.aiGenerationDisabledMessage`（`Views/ResumeInspectorViews.swift:44-48`）
→ 在 `sectionEditor(for:)` 里被**急切求值**并传给每个非基础分区编辑器
（`:113/:121/:129/:137`），且 `generate` 在 `:145` 也会求值。

**触发步骤**：在「工作年限」里输入/粘贴 ≥19 位数字（基础信息分区展开时不会崩溃），
然后展开「教育经历」或点击其 AI 生成按钮 → 硬崩溃，未保存的简历内容全部丢失。

**复现证据**（退出码 133，SIGTRAP）

```
Swift/arm64e-apple-macos.swiftinterface:45315: Fatal error: Double value cannot be
converted to Int because the result would be greater than Int.max
EXIT=133
```

顺带一提：18 位输入虽不崩溃但已被 Double 舍入失真
（`999999999999999999` → `1000000000000000000`），
这本身就说明用 `Double` 解析整数是不该走的路。

**修复建议**：转换前加上界（并按业务加上合理上限，例如 100 年）：

```swift
guard value >= 0, value < Double(Int.max) else { return nil }
return Int(value.rounded(.down))
```

---

### 4.2 高（High）

#### H-1 壁纸页：加载中的筛选变更被静默丢弃，网格显示"没有找到壁纸"
**`[已确认]`** — `Sources/Jarvis/Wallpaper.swift:1218-1222`

```swift
func refresh() async {
    searchGeneration += 1            // ← 先自增
    let generation = searchGeneration
    guard !isLoading else { return } // ← 再因为正在加载而直接返回
    ...
    guard generation == searchGeneration else { return }   // :1239 / :1244 旧请求自我作废
```

第二次 `refresh()`（用户在首次加载期间改了分辨率/比例/排序/标签）把 generation 加到 2
后立刻返回；第一次请求恢复时发现 `generation(1) != searchGeneration(2)`，
在**给 `items` 赋值之前**返回。结果：新筛选没发请求、旧结果被丢弃、
`items` 与 `errorMessage` 同时为空，网格渲染 `JarvisEmptyState("没有找到壁纸")`
——**一次成功的搜索被显示成"没有找到壁纸"**，用户必须再改一次筛选才能恢复。

**修复建议**：只在真正发起加载的路径上自增 generation；`isLoading` 时置 `pendingRefresh`
标志，在 `defer { isLoading = false }` 之后重跑。

---

#### H-2 更新失败会先毁掉屏幕录制/辅助功能授权
**`[已确认]`** — `Sources/Jarvis/AppVersion.swift:229-232`

```swift
let keepsCodeIdentity = JarvisLocalSigning.isAvailable
if !keepsCodeIdentity {
    try resetPrivacyPermissions()      // ← 在第 250 行下载之前就执行了
}
```

`resetPrivacyPermissions()` 执行 `tccutil reset ScreenCapture/Accessibility com.jarvis.mac`，
而它之后的每一步都可能抛错：下载（`:250`）、`verifyDigest`（`:259`）、`ditto` 解压（`:263`）、
`findApplication/isValidApplicationBundle`（`:268-272`）。任何一步失败，**当前正在运行的
Jarvis 已经永久失去两项授权**，而界面只提示"更新失败"。

代码注释给出的理由（"必须趁本进程还匹配 TCC 条目时清除"）并不成立：`tccutil` 是独立
二进制、直接改用户 TCC 数据库，在交接给安装脚本之前的任何时刻执行效果都一样。

> 说明：这条存在**审计意见分歧**。另一位审计员认为作者在 `AppVersion.swift:220-232`
> 与提交 `a053596` 中已明确记录该设计意图，故未上报。我保留了此条，因为它是否为"有意设计"
> 与"用户是否会因一次下载失败而失去授权"是两个问题——后者可以靠**仅调整顺序**完全避免。
> 如果维护者确认这是刻意取舍，可降级为 Low 并在代码里补一句真实理由。

**修复建议**：把 `resetPrivacyPermissions()` 移到 `makeInstallerScript(...)` 之前
（即校验/解压都通过、确认可安装之后）。

---

#### H-3 回滚会把旧应用搬进"半成品 bundle"内部（两处脚本）
**`[已确认]`** — `Sources/Jarvis/JarvisLocalSigning.swift:224-228`；`Sources/Jarvis/AppVersion.swift:614`

```swift
/bin/mv "$app" "$work/Replaced.app" || fail "移开旧应用失败"
if ! /usr/bin/ditto "$work/Signed.app" "$app"; then
    log "写入新应用失败，回滚"
    /bin/mv "$work/Replaced.app" "$app"     // ← $app 此时已被 ditto 建成目录
    fail "写入新应用失败，已回滚到原版本"
fi
```

`ditto` 失败时**会留下一个部分写入的目标目录**，而 POSIX `mv src dst` 在 `dst` 已存在且
是目录时，会把 `src` **移进 `dst` 里面**并返回 0。于是回滚结果是
`…/Jarvis.app/Replaced.app`——旧应用被封在残缺 bundle 内部，脚本却报告"已回滚到原版本"
并去 `open` 这个坏 bundle。

`AppVersion.swift:614` 的授权路径同一写法（AppleScript 里 `mv old backup && ditto new old;
else mv backup old`，回滚前无 `rm -rf`），而同一文件 `:551` 的 `restore_user_owned()`
却**正确地**先 `rm -rf` 了。两条路径自相矛盾，说明是遗漏而非设计。

**修复建议**：回滚前先 `rm -rf "$app"`（授权路径同理），并检查 `mv` 返回值。

---

#### H-4 安装变更后的 TCC 清理是死代码，永远不执行
**`[已确认]`** — `Sources/Jarvis/FreshInstallPermissionCleanup.swift:6-16`

```
$ grep -rn "JarvisFreshInstallPermissionCleanup" Sources/ Tests/
Sources/Jarvis/FreshInstallPermissionCleanup.swift:3:enum JarvisFreshInstallPermissionCleanup {
Tests/JarvisTests/AppVersionTests.swift:109,121,128      ← 只有测试引用
```

`runIfNeeded()` 在 `JarvisApp` / `AppModel.init` 里**没有任何调用点**，
整套 inode+mtime 指纹机制从未生效。全 App 的 TCC 重置只剩"更新流程"和"本机签名卡片"两条路。

**影响**：README 明确宣传的"手动拖入「应用程序」覆盖安装"升级路径，会留下旧 ad-hoc
身份写下的 TCC 记录。新二进制在系统设置里显示"已授权"，而
`CGPreflightScreenCaptureAccess()` / `AXIsProcessTrusted()` 返回 false，
且 macOS 通常不会再弹窗——用户只能手工删掉那条陈旧记录。

**修复建议**：在 `applicationDidFinishLaunching`（或 `AppModel.init`）中调用；
同时注意"本机签名身份存在时不能清"（`if !JarvisLocalSigning.isAvailable`），
否则会误伤本机签名升级路径（该函数当前没有这层判断）。

---

#### H-5 娱乐页：两个重叠下载会拿到同一目标路径并互相覆写
**`[已确认]`** — `Sources/Jarvis/EntertainmentVideoDownload.swift:554` + `Sources/Jarvis/AIConversationDownloads.swift:63-64`

文件名分配只检查 `fileExists(candidate)`。而 yt-dlp 全程写 `<dest>.part`，
只有在**下载结束**那一刻才改名成 `<dest>`——也就是说下载 #1 的整个生命周期内
`fileExists(dest)` 都是 false。文件名既无画质也无 UUID，下载按钮在下载期间保持可用，
因此"同一视频先下 1080p、再切 720p 下"这种最自然的操作就会让两个 yt-dlp 进程
写同一个 `.part` 并争抢改名。

**影响**：产物损坏/截断；"已完成"的条目指向另一个下载的字节，或指向一个已被
败方进程改名覆盖掉的文件。用户只在播放时才发现。

**修复建议**：用 `FileManager.createFile(atPath:)` 原子占位（失败时删除），
或在 manager 里维护已分配目标集合；同时在同一视频/画质下载进行中禁用下载按钮。

---

#### H-6 取消/失败的下载残留 `<name>.part`，下一次下载会"续传"进这个残file
**`[已确认]`** — `Sources/Jarvis/EntertainmentVideoDownload.swift:274`、`:697-704`

取消走 `process.terminate()`（SIGTERM），Python 捕获不到，yt-dlp 来不及清理 `.part`；
普通失败同样会留下它（这正是 yt-dlp 的续传设计）。代码里**没有任何一处**删除过这个
部分文件（文件内唯一的 `removeItem` 都指向 cookie 临时文件），目标路径分配器只看
`<dest>` 不看 `<dest>.part`，`--no-continue` 也从未传入。

**触发**：取消一个 1080p 下载（留下 `V.mp4.part`）→ 再下同一视频的 720p →
yt-dlp 打印 "Resuming download at byte N"，把 720p 的字节追加在截断的 1080p 数据之后。

**影响**：App 报告"已完成"的 MP4 实际无法播放；`~/Downloads` 里堆积用户看不见的部分文件。

**修复建议**：取消与失败路径删除 `destination` 与 `destination.path + ".part"`，或传 `--no-continue`。

---

#### H-7 媒体键模式下 F1/F2 的映射互相颠倒
**`[已确认]`** — `Sources/Jarvis/ShortcutManager.swift:57-58`（测试 `Tests/JarvisTests/ShortcutTests.swift:29-51` 固化了同样的错误值）

```swift
case 2: return 122 // Brightness down = physical F1
case 3: return 120 // Brightness up = physical F2
```

SDK 头文件（`MacOSX27.0.sdk/.../IOKit.framework/Headers/hidsystem/ev_keymap.h:56-59`，本机实测）：

```
#define NX_KEYTYPE_SOUND_UP        0
#define NX_KEYTYPE_SOUND_DOWN      1
#define NX_KEYTYPE_BRIGHTNESS_UP   2
#define NX_KEYTYPE_BRIGHTNESS_DOWN 3
```

常量名跟随**物理按键的作用**（F1 降低亮度、F2 提高亮度），
所以 keyType 2 属于 F2（kVK_F2 = 120），keyType 3 属于 F1（kVK_F1 = 122）——当前两个分支写反了。

**影响**：在 macOS 默认设置（"将 F1、F2 等键用作标准功能键"未勾选）下，
按键只以 `NX_SYSDEFINED` 事件到达此函数。而 `ScreenshotShortcut.default` 是 F1/122、
`clipboardDefault` 是 F2/120，于是：**按物理 F1（标签是"截图"）会打开剪贴板面板，
按物理 F2 会触发截图遮盖全屏**。这是笔记本上的默认配置。

**修复建议**：交换返回值（`case 2: return 120`、`case 3: return 122`），
同时修正注释与 `ShortcutTests` 的期望值。
附带存疑项：`case 32`（Mission Control）在公开头文件中没有对应常量
（`NX_NUMSPECIALKEYS` 为 24），F3 的会议快捷键在媒体键模式下是否真的有效，需要在真机确认。

---

#### H-8 预发布版本号被判为"更新"
**`[已复现]`** — `Sources/Jarvis/AppVersion.swift:316-321`

```swift
private func versionParts(_ version: String) -> [Int] {
    version
        .trimmingCharacters(in: CharacterSet(charactersIn: "vV"))
        .split(separator: ".")
        .compactMap { Int($0) }        // ← 丢弃而非停止
}
```

`compactMap` 会**丢弃**非数字段并把数字重新拼起来，跨过被丢掉的那一段。实测：

```
1.4.0-rc.1 vs 1.4.0 : true      （解析成 [1,4,1] —— "0-rc" 被丢弃，末尾 ".1" 存活）
1.4.0+build.7 vs 1.4.0: true    （解析成 [1,4,7]）
1.4.0-rc1 vs 1.4.0  : false     （解析成 [1,4]，补齐后相等）
release-2026 vs 1.3.5: true
```

`/releases/latest` 只排除 GitHub 上勾了 pre-release 的发布，因此维护者若打了
`v1.4.0-rc.1` 而未勾选，App 会提示所有稳定版用户升级到该预发布包。

**修复建议**：遇到第一个非数字段就 `break`；把 `-` 之后视为预发布（不高于同号版本）；
在提示更新前用 `^v?\d+(\.\d+)*$` 校验 tag。

---

#### H-9 历史截图编辑：`canvasSize` 用"适配后尺寸"而 `selectionRect` 用"原图尺寸"
**`[已确认]`** — `Sources/Jarvis/ScreenshotCaptureController+Presentation.swift:452-459`

```swift
let frame = PreviewWindowSupport.fittedFrame(for: image.size, in: visibleFrame)
let capture = ScreenshotCapture(data: data, screenFrame: frame)
let session = ScreenshotEditingSession(
    id: UUID(),
    frozenScreen: capture,
    selectionRect: CGRect(origin: .zero, size: image.size),   // ← 原图尺寸
    initialCapture: capture
)
```

`showResult` 用 `canvasSize = capture.screenFrame.size`（**适配后**，例如 2681×1508）
搭配 `outputRect = session.selectionRect`（**未适配**的 3008×1692），
于是 `coordinateSpace.canvasRect(fromOutputRect:)` 产出
`(0, canvasH − imageH, imageW, imageH)`——比画布更大且 `minY` 为负。触发条件：
历史截图比可见区域大（即几乎所有全屏截图）。

**影响**（均由同一处越界矩形推导）：
(a) `ScreenshotSelectionOverlay` 画不出边框/手柄，even-odd 遮罩把整块画布掏空；
(b) `movedRect`（`ScreenshotCanvasWindow.swift:296-301`）算出的
`min(max(v, 0), bounds.maxX − rect.width)` 出现 `upper < lower`，首次拖动会把选区**跳**到画布外；
(c) 该次拖动会把 `hasVisualEdits` 置真（幻影编辑），保存时多走一次重编码；
(d) 翻译取词范围按越界矩形缩放，例如 1000→736 的适配下，归一化 x=0.5 的文本块落到
x=500 而非 368，且宽高偏大约 36%，译框与原文错位；
(e) 贴图用 `selectionFrame(on:)` 得到未适配尺寸，可能超出屏幕。

**修复建议**：`selectionRect: CGRect(origin: .zero, size: frame.size)`
（或反过来让 `canvasSize` 等于原图尺寸并把窗口按比例缩放）。

---

### 4.3 中（Medium）

#### M-1 缓存目录迁移在 MainActor 上同步拷贝整个缓存（最长可达 10 GB）
`Sources/Jarvis/AppModel+Clipboard.swift:412-443`；实现 `ClipboardCacheStore.swift:652-735`

`chooseClipboardCacheDirectory()` 是 `@MainActor`，`migrateManagedFiles` 在
`lock.withLock { ... try fileManager.copyItem(...) }` 里逐文件同步拷贝。
拷贝期间主线程完全冻结（界面不重绘、剪贴板轮询停摆），所有并发抓取写入阻塞在锁上。
中途强退会留下半迁移目录（回滚只在"保存失败"时触发）。

**修复建议**：迁移放后台（store 本身线程安全），只把结果回主线程赋值；逐文件之间释放锁；补进度提示。

---

#### M-2 300 条上限无视"收藏"，会静默删掉收藏内容及其文件
`Sources/Jarvis/AppModel+Clipboard.swift:75-89`

`clipboardItems` 只按 `createdAt` 排序，溢出尾部直接取"最旧的"，**不看 `isPinned`**，
随后连文件一起删。而其它清理路径都明确保护收藏：`trimClipboardCacheIfNeeded` 过滤
`!$0.isPinned`（`:464`），手动清理遇到 `.favorites` 会拒绝并提示
"已收藏内容只能在剪贴板模块中手动清理"（`:323`）。只有这个 300 条上限例外。

**修复建议**：保留全部收藏 + 最新 `(300 − 收藏数)` 条未收藏；只把被丢弃的未收藏条目交给溢出清理。

---

#### M-3 缓存接近满时图片被静默丢弃，且日志标签误导
`Sources/Jarvis/AppModel+Clipboard.swift:63-73`；`ClipboardService.swift:583-585`；`AppModel.swift:381`

`prepareCacheSpace` 被接成空实现 `{ _ in }`（`AppModel.swift:381`），于是 `storeData`
在校验 `usedBytes + dataSize <= maximumBytes` 失败时直接返回 nil，条目 `imagePath` 为 nil
→ `hasLocalContent` 为 false → 在 `:73` **提前 return**，连后面的
`trimClipboardCacheIfNeeded()` 都走不到。而淘汰策略恰恰把用量维持在容量边缘，
所以这个窗口是常态。

日志把结果标成 `"unusableDuplicate"`（`:65-71`），但真实原因是容量，排障时会被带偏。
全程没有 toast——用户复制了一张图片，它**从未出现**，且没有任何解释，
违反项目自定的"持久化失败必须记录**并且**对用户可见"。

对比：文本走内联 `text` 所以幸存，文件退化成外部引用，**只有图片会丢**。

**修复建议**：真正实现 `prepareCacheSpace`（先腾空间再写），拒绝时给 toast 并修正日志 `result`。

---

#### M-4 容量上限不是原子预留，并发写入会超限
`Sources/Jarvis/ClipboardCacheStore.swift:282-296`、`:357-366`

容量判断在锁内、字节写入在锁外，`usageLocked()` 只统计"已落盘的文件"。
粘贴两个文件会各自 dispatch 一个 global queue 任务（`ClipboardService.swift:398-400`），
TIFF 转换同样异步，因此两个在途写入会看到同一份"写入前用量"并双双通过。
注释 `AppModel.swift:378-379` 声称 `ClipboardCacheStore enforces the hard byte limit atomically`
——**这句话与实现不符**。

**修复建议**：锁内预留字节（`reservedBytes` 计入算式，`defer` 释放），或把锁持有到写入结束。

---

#### M-5 每次拖动容量滑杆都会在 MainActor 上全量重扫缓存
`Sources/Jarvis/AppModel+Clipboard.swift:259-281`；`Views/ClipboardCacheSettingsCard.swift:94-100`

滑杆 setter → `updateClipboardCacheMaximumBytes` → `usage()`（递归枚举 + 逐文件 stat，
`:805-837`）同步跑在主线程，紧接着 `trimClipboardCacheIfNeeded` 再扫一次，
`refreshClipboardCacheUsage` 第三次。这正是提交 `d7fde4c` 从淘汰循环里拿掉的成本，
却在设置页保留了。另外 `refreshClipboardCacheUsage`（`:249-257`）没有 generation 检查，
较旧的扫描可能后到并覆盖较新的值，**用量读数会往回跳**。

---

#### M-6 换缓存目录后在整棵目录树上统计与删除，可能删到无关文件
`Sources/Jarvis/ClipboardCacheStore.swift:805-837`、`:551-610`；`AppModel+Clipboard.swift:404-412`

两个 enumerator 都没有 `.skipsSubdirectoryDescendants`，而缓存目录是用户可选的
（`NSOpenPanel` 不限制且无警告）。用户把缓存目录选成「文档」或「桌面」后：
(1) `usage()` 把所有无关文件计进用量 → 任何容量调整都被拒，每次抓取都触发一轮
永远无法达标的"整段历史淘汰"；(2) `removeOrphanedManagedFiles` 会删除树中任何名为
`item-*` / `migrated-*` 的无关文件。

**修复建议**：维护 App 自建文件清单，或把枚举/删除限制在所选目录下的私有子目录内；面板中明确提示。

---

#### M-7 启动时登录项被静默重新注册，用户无法真正关闭
`Sources/Jarvis/AppModel+Settings.swift:349-361`、`AppModel.swift:305`；`LaunchAtLoginService.swift:21-27`

```
$ grep -rn "SMAppService" Sources/Jarvis/
Sources/Jarvis/LaunchAtLoginService.swift:22:        try SMAppService.mainApp.register()
Sources/Jarvis/LaunchAtLoginService.swift:26:        try SMAppService.mainApp.unregister()
```

**`SMAppService.mainApp.status` 全工程从未被读取**，而 `synchronizeLaunchAtLogin()`
在每次启动时从 `AppModel.init` 调用一次，只要持久化偏好还是 `true` 就无条件 `register()`。
用户在「系统设置 → 通用 → 登录项」里手动移除贾维斯后，下一次启动会把它**再加回来**。
设置页开关也只反映持久化偏好，不反映真实注册状态。

**修复建议**：以 `SMAppService.mainApp.status` 为真值来源，只在用户显式开启时注册。

---

#### M-8 `install.sh` 覆盖安装前不确认旧进程已退出
`install.sh:68-73`、`204-215`

退出是"发完就算"（`|| true`，无存活性检查），只有固定 1 秒宽限。`osascript` 可能直接失败
（自动化授权被拒或弹窗），进程也可能因模态面板、会议录音收尾超过 1 秒。脚本随后
`rm -rf` 掉正在运行的 bundle 再拷新的，最后的 `open`（无 `-n`）会让 LaunchServices 激活
**仍在运行的旧实例**——其 bundle 已不存在。用户被告知"已安装并已启动"，实际还停在旧版本。

App 自身更新流程正是用 `open -na` + `waitForParentExit` 规避这个问题的，
`install.sh` 是唯一没有等待的路径。

---

#### M-9 `window.open` 弹窗是全尺寸子视图，且没有关闭途径
`Sources/Jarvis/JarvisWebPlatform.swift:598-615`

```swift
let popup = WKWebView(frame: webView.bounds, configuration: configuration)
...
webView.addSubview(popup)       // ← 不是窗口，是盖在页面上的全尺寸子视图
...
func webViewDidClose(_ webView: WKWebView) {    // ← 唯一的移除路径
    webView.removeFromSuperview()
```

弹窗只在被打开的页面自己调用 `window.close()` 时才消失。任何不自关闭的脚本弹窗
（广告/同意弹窗、lightbox）会**永久盖住**整个浏览内容，同时被 `popupWebViews` 强引用，
每个都是一个活的 WebContent 进程。界面没有关闭入口，切走再切回来弹窗仍在，用户只能重启 App。

**修复建议**：用真正的子 `NSWindow`/面板承载并带关闭按钮，或至少提供显式关闭入口与数量上限。

---

#### M-10 WebContent 进程终止从未处理
`Sources/Jarvis/JarvisWebPlatform.swift:459`（`WKNavigationDelegate` 扩展）

全仓库没有 `webViewWebContentProcessDidTerminate`（grep 只有 `webViewDidClose`），
而 WebContent 进程被杀**不会**产生导航失败回调，因此 `loadState` 停在 `.loaded`，
失败遮罩不出现（`JarvisWebPlatformViews.swift:331` 只响应 `.failed`），也不会自动重载。

App 会长期保活最多 4 个娱乐 + 4 个 AI 提供方 WebView（每个一个内容进程）并播放视频，
内存压力下杀进程是常态。用户回到该平台时看到**白板**，没有任何解释。

**修复建议**：实现 `webViewWebContentProcessDidTerminate(_:)` → 置 `.failed(...)` 或 `reload()`。

---

#### M-11 "另存副本"把一切失败都 `try?` 吞掉
`Sources/Jarvis/EntertainmentVideoDownload.swift:630-633`

```swift
panel.begin { response in
    guard response == .OK, let destination = panel.url else { return }
    try? EntertainmentVideoFileActions.copyFile(at: source, to: destination)
}
```

`copyFile` 的失败条件真实可达：目标卷只读/可移动介质、磁盘写满、源文件在 `:624-625`
的存在性检查与拷贝之间被删（同名重下、或 H-5 的竞态）、权限拒绝。全部被丢弃：
不记日志、不提示、界面不动。用户以为导出成功，磁盘上没有文件。

---

#### M-12 Cookie 导出按裸后缀匹配域名，会把无关 Cookie 导给第三方 CLI
`Sources/Jarvis/EntertainmentVideoCookies.swift:26,31-34`

```swift
return domain.hasSuffix("youtube.com")
    || domain.hasSuffix("youtu.be")
    || domain.hasSuffix("google.com")            // ← 同时命中 notgoogle.com
    || domain.hasSuffix("googleusercontent.com")
```

后缀判断没有标签边界，因此 `notyoutube.com`、`evilyoutube.com`、`fakeyoutu.be`、
`notgoogle.com` 的 Cookie 都被判为"相关"；YouTube 分支还会导出**全部** `.google.com`
（含广告 `NID`、其它 Google 服务会话）与 `*.googleusercontent.com` 的 Cookie。其中
HttpOnly 的会被序列化成 `#HttpOnly_` 前缀（yt-dlp 会真的带上），以明文写入
`jarvis-cookies-<uuid>.txt` 并交给第三方命令行，且该文件在整个下载期间一直留在临时目录。

需要校准：yt-dlp 的 cookie jar 自身按域过滤，所以危害是**明文过度导出 / 最小权限被破坏**，
而不是跨域外发。

**修复建议**：改成标签级比较（`domain == "youtube.com" || domain.hasSuffix(".youtube.com")` 等）。

---

#### M-13 会议：转码后立刻删除系统音频 CAF，但新文件名要等转写结束才落盘
`Sources/Jarvis/AppModel+Meetings.swift:526-533`（引用点在 `:373`、`:387`）

```swift
try await MeetingAudioMixer.transcodeToM4A(inputURL: currentSystemURL,
                                          outputURL: recordingURLs.systemCompressed)
MeetingAudioMixer.removeFileIfExists(at: currentSystemURL)   // ← 先删
systemAudioFileName = recordingURLs.systemCompressed.lastPathComponent
```

新文件名直到 `processMeeting` 里才写进记录（`:373` 赋值、`:387` 落盘），
而那是在 `transcribe(...)` **返回之后**——对于一小时的会议，这个窗口有几分钟。
触发：转写期间退出/崩溃（`applyInterruptedLaunchRecovery` 把 `.transcribing` 标成 `.failed`），
或任何 ASR/AudioConverter 错误。

**影响**：重试时 `prepareAudioForTranscription` 读到的是已经不存在的 `.caf`，
`MeetingAudioMixer.makeMixedAudio` 看到系统音轨不可用 → **用纯麦克风副本覆盖已有的混音文件**，
而那个已转码好的 `meeting-<id>.system.m4a` 成为任何代码路径都引用不到的孤儿。
**远端参会者的发言永久丢失**。

**修复建议**：在"已持久化引用压缩文件名的记录"之后再删 CAF
（即先 persist 更新后的 `systemAudioFileName` 再 transcribe），
或让 `systemAudioURL(for:)` 在 `.caf` 缺失时回退到 `.system.m4a`。

---

#### M-14 会议：停止/开始竞态，未完成的 stop 会清掉新会话的采集状态
`Sources/Jarvis/AppModel+Meetings.swift:139-161`；`Sources/Jarvis/MeetingRecorder.swift:90-97`、`:230-243`、`:336-342`

```swift
meetingCurrentRecordingID = nil            // AppModel+Meetings.swift:148 — UI 已显示"未在录音"
...
recorder = nil                             // MeetingRecorder.swift:94 — isRecording == false
let stats = await systemAudioRecorder.stop()   // :97 — 在这里挂起
```

```swift
try? await stream.stopCapture()
writeQueue.sync {}
let stats = captureStats()
clearCaptureState()                        // :241 — 清掉"当前"的 stream/outputURL
```

没有任何东西串行化 `stop()` 与 `start()`。在 `:148` 到 `stop()` 结束之间，
App 层状态说"未在录音"，于是再按一次录制（按钮或热键）会启动会话 B：
B 的 `setCaptureState(stream: B, outputURL: urlB)` 可能落在 A 尚未执行的
`clearCaptureState()` **之前**，那个 clear 会把 B 的 `outputURL` 抹掉，
之后所有系统音频缓冲都被 `guard let outputURL = lockedOutputURL() else { return }`（`:261`）丢弃。

**影响**：第二次会议收不到系统音频（或只收到开头一瞬），而 UI 报告正在采集；
停止时还会因为 `currentStream() == nil` 返回过期统计。

**修复建议**：stop 在途时禁止 start（`isStopping` 标志，或把 `meetingCurrentRecordingID`
保留到 `finishStoppedMeeting`）；`clearCaptureState` 只清"确实属于自己那次"的状态。

---

#### M-15 会议：24s 分片超出 128 token 解码预算，且 2s 重叠去重让丢失的尾部无法补救
`Sources/Jarvis/MeetingTranscriptionService.swift:72-97`

FluidAudio 的 Paraformer 管线每次调用最多解码 128 个 token——
本机 vendored 源码实测：`ParaformerConfig.decoderMaxTokens = 128`
（`ParaformerConfig.swift:20`）且 `ParaformerManager.swift:69,106` 用
`min(embeds.count, ParaformerConfig.decoderMaxTokens)` 截断。超出部分被**静默丢弃**。
24 秒分片在密集语速（>≈5.3 字/秒）或两人重叠时就会触顶；而每片又会丢掉开头 2 秒
（已由上一片解码），于是该片没能解出的区域**永远不会被重新解码**，每片最多约 3 秒空洞。

编码器侧没问题（24s ≈ 400 帧 < `decoderEncFrames = 512`）。

**修复建议**：把 `chunkSize` 降到约 15 秒（15 × 6 字/秒 = 90 token，安全低于 128）并保留 2 秒重叠。

---

#### M-16 会议：中文 ASR 模型下载期间进度冻结在 52%
`Sources/Jarvis/MeetingTranscriptionService.swift:37-41`

```swift
if chineseASR == nil {
    progress(.chineseTranscription, 0.52)
    chineseASR = try await ParaformerManager.load(precision: .int8)   // ← 没传 progressHandler
}
progress(.chineseTranscription, 1)
```

`ParaformerManager.load` 接受 `progressHandler` 并转发给 HuggingFace 下载，但这里没传，
所以 0.52 与 1.0 之间不会有任何回调。约 600 MB 的下载会让 UI 显示"准备中文转写模型 52%"
冻结数分钟，看起来像卡死，用户会取消或重试。

---

#### M-17 会议：只含 `openQuestions` 的总结被判为空而失败
`Sources/Jarvis/MeetingSummaryService.swift:326-328`

```swift
guard !overview.isEmpty || !keyPoints.isEmpty || !decisions.isEmpty || !actionItems.isEmpty else {
    throw AIAPIError.emptyGeneratedContent(context: "会议总结")
}
```

`openQuestions` 在 `:325` 被解析并保存，却不计入"有内容"。一个合法响应
（overview 为空、三个数组为空、`openQuestions` 有一条）会抛错，整个总结流程被标记失败，
内容被丢弃。**修复建议**：把 `|| !openQuestions.isEmpty` 加进 guard。

---

#### M-18 会议搜索会漏掉所有"详情未加载"的记录
`Sources/Jarvis/MeetingModels.swift:310-317`、`AppModel+Meetings.swift:224-231`、`Views/MeetingContentViews.swift:274-280`

`MeetingRecord.metadataCopy` 刻意清空 `transcript`，启动时只加载元数据，
而详情只为**选中**的那条惰性加载（`ensureMeetingDetailLoaded`）。
因此冷启动后内存里只有一条记录的逐字稿，搜索命中其它会议的逐字稿会返回"没有匹配的会议"。

**影响**：静默假阴性——会议存在却被报告为不存在。
**修复建议**：查询非空时先加载/扫描磁盘上的详情文件，或维护一个小的搜索索引。

---

#### M-19 更新失败对用户完全不可见
`Sources/Jarvis/AppVersion.swift:33-41`；`AppModel.swift:615-617,641-643`；`Views/SettingsContentView.swift:204-263`

```swift
updateState = .failed(message: error.localizedDescription)
```

而唯一的消费者 `switch app.updateState` 只处理 `.available/.checking/.downloading/.installing`，
`default` 分支丢弃了 message；`checkForUpdates` / `downloadAndInstallUpdate` 也都没有
`showToast`。触发：离线/被限流的检查、`badServerResponse`、`checksumUnavailable`、
`checksumMismatch`、`invalidApplication`、`unsupportedInstallLocation`、工具失败等。
失败后"下载新版本"按钮消失且无解释。这条同时掩盖了 H-8 的失败表现。

---

### 4.4 低（Low）

| 编号 | 问题 | 位置 |
| --- | --- | --- |
| L-1 | 更新路径丢弃 `resign` 的返回值：证书在下载期间消失时仍会装入未重签的 ad-hoc 包，授权静默失效（注释却承诺"失败即中止"） | `AppVersion.swift:274-280`；`JarvisLocalSigning.swift:49-50` |
| L-2 | `install.sh` 在没有 `digest` 字段时**跳过**校验直接安装；App 自身对同一情况是硬失败（`checksumUnavailable`）。README 声称已校验校验和 | `install.sh:126-130` vs `AppVersion.swift:365-368` |
| L-3 | `install.sh` 用贪心正则从**单行** GitHub JSON 里取 URL/digest，实际取到的是**最后一个** asset 的值，而选择 asset 用的是第一个匹配——两者可能不是同一个 | `install.sh:113-119` |
| L-4 | `build_app.sh` 的 `cp .../*.png` 没有 zsh `(N)` 空匹配限定符：`AIProviderIcons` 只剩一个 PNG，一旦转成 SVG 就会以 `no matches found` 让所有打包/CI 构建失败（本机已实测该 zsh 行为） | `build_app.sh:45-47`、`:116` |
| L-5 | 壁纸 metadata 写失败时，已全尺寸拷贝进目录的图片成为永久孤儿（不可见、不可删、不回收）；`ScreenshotHistoryStore.add` 对同一场景有回收 | `Wallpaper.swift:536-544` |
| L-6 | 收藏/删除失败在「已下载」「收藏」标签页完全不可见：`errorMessage` 只在 `onlineGallery` 且 `items.isEmpty` 时渲染；`delete` 还先删文件后写 metadata | `Wallpaper.swift:1318-1320/1330-1332`、`:555-562`；`Views/WallpaperContentViews.swift:264,392-400` |
| L-7 | 壁纸网格 body 每次重绘都对每个卡片做 `localURL(for:)`（isSafeFileName + 两次 URL 归一 + fileExists + 解析图片头 + resourceValues） | `Views/WallpaperContentViews.swift:312,490`；`Wallpaper.swift:506-525` |
| L-8 | 缩略图路径用 `Data(contentsOf:)` 读入整个原图（常见 2–40 MB）再缩到 ≤512px；打开「已下载」可同时读入约 36 张原图 | `Wallpaper.swift:1440` |
| L-9 | `JarvisThumbnailCache` 成本单位是像素而限额单位是字节（同工程 `WallpaperImageLoader` 用的是 `bytesPerRow * height`），实际可驻留内存约为预算的 4 倍 | `ThumbnailCache.swift:6-9,53` |
| L-10 | 壁纸预览先整幅解码（8K ≈ 130 MB RGBA）再缩放到不超过屏幕尺寸 | `WallpaperPreviewController.swift:15`；`Wallpaper.swift:1407-1428` |
| L-11 | `Index.plist` 缺失/不可读会抛错，连"只设置桌面壁纸"这条不需要 plist 的路径也一起失败 | `Wallpaper.swift:807-811` |
| L-12 | 缩略图加载失败后卡片永久显示 `ProgressView`：`.task(id: url)` 不会重跑，也没有失败/重试状态 | `Views/WallpaperContentViews.swift:642-659` |
| L-13 | 工具栏"下载中"角标永不更新：manager 是 Combine `ObservableObject`，却在 `@Observable AppModel` 上标了 `@ObservationIgnored`，视图也没有 `@ObservedObject`（娱乐与 AI 两处同型） | `Views/EntertainmentView.swift:27`、`Views/AIConversationView.swift:106`；`AppModel.swift:138-144` |
| L-14 | 文件名净化可能产出空基名 → 隐藏点文件（标题恰为 `.mp4` 时结果是 `.mp4`）；控制字符/换行未清理 | `EntertainmentVideoDownload.swift:333-338` |
| L-15 | 下载历史保存失败只记日志、不告知用户（`write` 返回值被丢弃） | `EntertainmentVideoDownloadHistory.swift:133-135` |
| L-16 | `noVideo` 错误不可达：`best` 选项无条件加入，纯音频/图片页仍显示「最佳画质」 | `EntertainmentVideoDownload.swift:125-135,244` |
| L-17 | 取消"排队中"的下载无效：`attach` 找不到 pending 就重新入队并继续（含保存面板） | `AIConversationDownloads.swift:134-137,163-173` |
| L-18 | 快捷键录制器对 App 自身的全局热键透明：录制时按 F1 会真的触发截图（全屏遮罩）而非被录下；本地监视器 `return nil` 抢先消费事件 | `ShortcutManager.swift:364-387`；`Views/SettingsContentView.swift:93-113` |
| L-19 | 快捷键录制器失焦后不退出录制态（无 `resignFirstResponder`、无点击外部监视器），界面一直显示"按下快捷键…" | `ShortcutManager.swift:506-547` |
| L-20 | 存储的 F2 截图快捷键每次启动都被强制改回 F1：迁移判断里 `shortcut == .legacyDefault` 落在了"只执行一次"的标志之外 | `AppModel+Settings.swift:499-509` |
| L-21 | 校验失败的快捷键仍显示在录制框里（不回滚本地 `@State`），且冲突提示可能残留到别的行 | `Views/SettingsContentView.swift:93-113`；`ShortcutManager.swift:513-541` |
| L-22 | `statusMessage` 被写 28 处、**没有任何视图读取**；`isCapturing` 写 3 处、完全未读。因此权限引导、快捷键保存确认等只靠这条通道的反馈全部静默 | `AppModel.swift:81-82`；`AppModel+Settings.swift:389/430/471` 等 |
| L-23 | 贴图（pin）后的编辑工具栏不可达：`showsToolbar` 全仓库从未被置真（`1805ff6` 曾有的右键菜单已消失），`showPinnedToolbar` 等一整条链路是死代码 | `ScreenshotSelectionWindows.swift:535`；`ScreenshotCaptureController+Pinned.swift:105-109` |
| L-24 | 渲染失败时 `finalPNGData()` 静默回退到**未标注**的原图，App 仍提示"已确认并复制/已保存"；马赛克等打码可能静默消失 | `ScreenshotEditor.swift:560-562,588-594` |
| L-25 | `showResult` 解码失败时直接 `return`，而 `selectionCompletionDelivered` 已置真、全屏遮罩仍在，Esc 也被守卫拒绝——截图功能被锁死到重启 | `ScreenshotCaptureController+Presentation.swift:183`、`:394-395` |
| L-26 | 空白处单击（无窗口候选）被当成"选区太小"取消整次截图，提示"请重新框选"但遮罩已关闭；桌面图标属于负 layer 窗口会被 `layer >= 0` 过滤掉，所以这正是常见情形 | `ScreenshotSelectionOverlayView.swift:154-160`；`ScreenshotCaptureController+Pinned.swift:355-362` |
| L-27 | 贴图创建失败仍上报成功（`createPinnedScreenshot` 可静默 return，调用方无条件 `onAction(.pin)`） | `ScreenshotCaptureController+Pinned.swift:18-22,31-33` |
| L-28 | 截图历史硬上限 100 条，超出后静默删除最旧文件，无任何提示 | `ScreenshotHistoryStore.swift:19,195-215` |
| L-29 | `trimmed()` 先删 PNG 再写 metadata：metadata 写失败后留下"索引有条目、文件已丢"的不一致状态 | `ScreenshotHistoryStore.swift:195-215` |
| L-30 | `clampedCanvasRect` 用 `intersection`，对"部分越界"的矩形不会收窄，与函数名及调用方"clamp"的意图不符 | `ScreenshotCoordinateSpace.swift:37-44` |
| L-31 | 会议：删除正在处理的会议会弹出伪造的失败提示（取消以 `URLError(.cancelled)` 呈现，`catch is CancellationError` 接不住），并留下陈旧的 `.failed` 状态 | `AppModel+Meetings.swift:273-276,396-400,613-618` |
| L-32 | 会议：模型准备失败在会议页不可见（`.failed` 只落到普通按钮）、且不写日志；失败文案只在设置页渲染 | `AppModel+Meetings.swift:36-39`；`Views/MeetingContentViews.swift:205-210` |
| L-33 | 会议：混音文件尚未生成时播放器直接报"未找到本机录音文件"，尽管麦克风录音存在（与"已保留原始录音"的恢复提示自相矛盾） | `AppModel+Meetings.swift:291-293`；`Views/MeetingContentViews.swift:653-661` |
| L-34 | 会议：`meetingProcessingState` 是全局单值且在任务出队前就被改写，选中另一条记录时会看到别人任务的进度；删除活动记录后状态不复位 | `AppModel+Meetings.swift:144-161,317-322,283-285` |
| L-35 | 会议：`loadDetail` 用 `try?` 吞掉读取/解码失败——正是 `JarvisJSONFile` 文档里警告的"读不出来绝不能变成后续写入的基础"，且损坏文件会被下次成功保存覆盖 | `MeetingRepository.swift:117-123` |
| L-36 | 会议：启动恢复的保存失败被 `try?` 吞掉（其它会议流程都会 toast），记录会每次启动重复恢复 | `AppModel.swift:240-242` |
| L-37 | API Key 的 Keychain 写入先 `delete()` 再 `SecItemAdd`，非原子：add 失败即永久丢 key；迁移路径还用 `try?` 吞掉失败而 legacy 条目已删 | `AIAPIClient.swift:665-678,633-642` |
| L-38 | `openAIBaseURL` 对"以 `/chat/completions` 结尾且无 `/v1`"的自定义端点强行补 `/v1`，导致设置页回显与再次保存会静默改写端点、且"获取模型"打到错误 URL | `AIAPIClient.swift:162-170`；`Views/AIAPISettingsCard.swift:307-325` |
| L-39 | 会议菜单项在有权限但模型未就绪时仍可点击（`menuWillOpen` 丢掉了 `meetingModelsReady` 条件） | `JarvisMenuBarController.swift:130-134` vs `:255-260` |
| L-40 | 截图历史预览对无法解码的图片静默无操作（无 toast、无日志） | `ScreenshotHistoryPreviewController.swift:8-14` |
| L-41 | 主窗口三个 block observer 只在 `detach()` 里移除、类无 `deinit`；`intersects` 判定下 1 点重叠即视为"在屏"，换显示器后窗口可能恢复到几乎不可见且无复位入口 | `JarvisMainWindowController.swift:180-186,193-224,246-250` |
| L-42 | 视频缩略图完成回调不检查取消/身份，LazyVGrid 复用可能短暂显示上一项的缩略图 | `Views/ClipboardItemPreview.swift:43-45,117-141` |
| L-43 | `ByteCountFormatter` 每次重绘重建（与 `5dc0a57` 已修的 `DateFormatter` 属同一类） | `ClipboardCacheStore.swift:873-878`；`AppModel+Clipboard.swift:526-532` |
| L-44 | 孤儿缓存文件不会主动清扫（崩溃前写入、debounce 未落盘、被拒图片的缩略图等），只在"仍超容量"或手动清理时回收 | `AppModel+Clipboard.swift:491-504` |
| L-45 | 截图粘贴板里非文本内容（如 HEIC/JPEG 图片）存在覆盖缺口，无法确认到具体发布者 | `ClipboardService.swift` 图片分支 |
| L-46 | `JarvisInstanceCoordinator.isPrimaryInstance` 是死字段（仅类内自用），锁机制不影响任何行为 | `JarvisInstanceCoordinator.swift:8` |
| L-47 | `LoggingTests` 把进程级日志运行时永久指向一个随即被删除的临时目录，`debugEnabled` 也永久置真，污染后续测试 | `Tests/JarvisTests/LoggingTests.swift:11-12` |
| L-48 | 仅在受限沙箱下暴露：`build_app.sh` 内的 `swift build -c release` 未加 `--disable-sandbox`，在需要嵌套 `sandbox-exec` 的环境里会直接失败 | `build_app.sh:32` |
| L-49 | 会议：模型缓存每次队列排空就被释放，于是下一次会议在 `transcribe` 内部重新准备模型，而准备进度用的是**正式阶段名**——UI 会从"正在转写录音 92%"倒退到"正在识别说话人 2%"再到"正在转写录音 50%" | `MeetingTranscriptionService.swift:49-63`；`AppModel+Meetings.swift:319-321` |
| L-50 | 会议：整段重采样音频被一次性读进单个 `[Float]`（16 kHz 单声道 Float ≈ 230 MB/小时），6–8 小时会议在加载模型前就要 ~1.4–1.8 GB；录音无时长上限，而说话人分离那条路径是流式读盘 | `MeetingTranscriptionService.swift:71-84` |
| L-51 | 会议：`MeetingLanguage` 被声明为可选但参数被丢弃（`language _:`），且没有任何视图引用它，所以今天不可达——属于"加了选择器就会立刻踩到"的潜伏陷阱 | `MeetingTranscriptionService.swift:47` |

---

## 5. 缺陷分布

| 严重度 | 数量 |
| --- | --- |
| 严重 | 4 |
| 高 | 9 |
| 中 | 19 |
| 低 | 51 |
| **合计** | **83** |

按子系统（含低危）：

| 子系统 | 严重 | 高 | 中 | 低 | 合计 |
| --- | --- | --- | --- | --- | --- |
| 剪贴板 / 缓存 | 2 | — | 6 | 5 | 13 |
| 截图 / 标注 / 翻译 | 1 | 1 | 3 | 8 | 13 |
| 会议记录 | — | — | 6 | 9 | 15 |
| 更新 / 签名 / 安装 | — | 3 | 2 | 5 | 10 |
| 娱乐 / 视频下载 / 内嵌 Web | — | 2 | 4 | 5 | 11 |
| 壁纸 | — | 1 | 3 | 7 | 11 |
| 简历 | 1 | — | — | — | 1 |
| 设置 / 热键 / 生命周期 | — | 1 | 3 | 8 | 12 |
| AI / 网络 | — | 1 | 1 | 5 | 7 |
| 基础设施（日志/缓存/测试） | — | — | — | 3 | 3 |

---

## 6. 审计方法与可信度

每条结论都走同一条路径：**读完整文件 → 追调用链确认可达 → 尽量用可执行试验坐实 →
复核是否为近期提交引入的回归 → 标注置信度**。

### 6.1 本次实际执行的验证

1. **全量构建 + 全量测试 + 覆盖率采集**：debug/release 均编译通过；
   `--enable-code-coverage` + `llvm-cov report` 产出逐文件数据。
2. **C-2 的 Swift 语义复现**：最小程序证明 `for i in a.indices` 在循环内缩容后越界 trap，
   退出码 **133（SIGTRAP）**。
3. **C-1 的端到端复现**：临时集成测试证明 `removeLegacyFiles` 会删除缓存目录之外的
   用户文件（`true → false`），已固化为回归用例。
4. **C-3 的像素级复现**：构造红/绿/蓝/白四带画布 + 矩形模糊，
   采样导出图与源图同一点的颜色，证明 mosaic 呈现的是镜像位置内容
   （源为蓝色、导出为绿色、且与 `canvasHeight − y` 处一致）。已固化为回归用例。
5. **C-4 的崩溃复现**：用源码中的同一表达式，`9999999999999999999` 触发
   `Fatal error: Double value cannot be converted to Int`，退出码 **133（SIGTRAP）**。
6. **H-8 的行为复现**：用源码中同一 `versionParts` 实现，
   证明 `isNewer("v1.4.0-rc.1", than: "1.4.0") == true`。
7. **L-4 的 zsh 行为复现**：实测 `set -euo pipefail` 下 `cp ./*.png` 无匹配时
   以 `no matches found` 退出 1，加 `(N)` 后正常。
8. **H-7 的依据核对**：直接读取本机 SDK 头文件
   `ev_keymap.h:56-59` 确认 `NX_KEYTYPE_BRIGHTNESS_UP == 2` / `..._DOWN == 3`。
9. **近期提交回归确认**：`git show a053596` 证明 C-1 是回归
   （原先用的是带守卫的 `removeManagedFiles`）；`git show f8aee79` 复核下拉菜单定位修复
   未引入新问题。

### 6.2 被证伪的候选（重要）

报告里的每一条都经过了复核，**没有把猜疑当成缺陷**。以下候选曾经看起来很像 bug，
但读码或实测后**否定**，一并列出以免后人重复排查：

- **截图导出裁剪区域上下翻转**：我最初的怀疑。写了像素级试验
  （红/蓝双色画布 + 编辑器选择矩形），实测导出区域与用户选择**一致**，假设否定。
- **mosaic 之外的渲染镜像**：底图走非镜像 CTM，矢量标注只映射点，均正确——
  只有 mosaic 位图受害（已列为 C-3）。
- **`JarvisJSONFile` 修订号守卫**（`revision >= appliedRevision`）：
  两个写入者的交错顺序逐一推演，去抖写入与同步写入不会拿到相同 revision，
  旧状态无法覆盖新状态。已写成防回归用例。
- **`ScreenshotHistoryStore` / `ClipboardCacheStore` 锁顺序**：单向
  （store 锁 → `JarvisJSONFile` 锁），无环、无重入。
- **`JarvisLogRedactor` 会把 `meetingID`（36 字符 UUID）误伤为 `<opaque>`**：
  UUID 被连字符切分成 ≤12 字符段，32 字符下限匹配不到。
- **`JarvisLocalSigning.swift:279` 的 `message!`**：前置
  `message?.isEmpty == false` 已保证非空。
- **`build_app.sh` / `install.sh` 的 `pipefail` + `grep -q` 组合**：
  macOS/BSD `grep -q` 会读完输入，不会 SIGPIPE。
- **`install.sh` 的 `trap cleanup EXIT` 覆盖退出码**：真实脚本的显式 `exit 0` 路径优先。
- **`JarvisInstanceCoordinator` 的 flock 被 `Process` 子进程继承**：实测未继承。
- **`AXUIElement` 所有权与多显示器 AX↔AppKit 坐标换算**（含负原点、上下左右副屏）：
  手工逐象限验算正确。
- **版本号一致性**：`1.3.5`/`332`/`com.jarvis.mac` 跨 Info.plist、`build_app.sh`、
  `AppVersion.swift`、README 全部一致。
- **发布包签名（v1.3.5 实测）**：`codesign --verify --deep --strict` 通过，
  entitlements 与 `Resources/Jarvis.entitlements` 完全一致，bundle 内无嵌套代码，
  因此省略 `--deep` 是安全的。
- **诊断导出包的脱敏**：事件日志在写入时已脱敏，manifest 只含计数/哈希/脱敏路径，无 API Key/PII 泄漏。
- **`NSImage.cgImage(forProposedRect:)` 的 DPI 缩放**：实测返回完整像素图，
  `pixelScale` 是真实的 pixels-per-point，Retina 没有被应用两次。
- **`NSHostingView.isFlipped == true`**：实测确认，所以
  `canvasPoint(for:)` 直接返回局部坐标是正确的，选择框移动/缩放方向没有反。
- **`NSPanel.isReleasedWhenClosed` 默认值**：实测为 `false`（只有普通 `NSWindow` 是 `true`），
  所以关闭遮罩面板时不会过度释放。
- **`EntertainmentVideoLink` 的约 30 种 URL 形态**（含 `youtube.com.evil.com`、
  `user@host` 混淆、`evil.com/?u=https://youtube.com/...`）：主机匹配按标签，无混淆。
- **`MeetingModelStorage.availability()` 的路径假设**：对照 vendored FluidAudio API 逐项核对正确。
- **会议遗留索引迁移会丢数据**：逻辑真实，但 `git tag --contains` 显示旧数组格式与
  新分文件格式**首次出现在同一个 tag（v1.2.24）**，没有任何已发布版本会写出旧索引，
  因此不可达，未列为缺陷。

### 6.3 已知局限

- 覆盖率是**单元测试**覆盖，视图层天然偏低，不应简单等同于"质量低"；
  本报告只把"**业务编排层零覆盖**"当作风险信号。
- 以下场景需要真实设备/网络/权限才能端到端执行，本次只能静态推断：
  音频采集与混音（`MeetingRecorder`）、真实 yt-dlp 调用、ScreenCaptureKit 抓屏、
  WebContent 进程崩溃、多显示器（本机只接了单显示器，H-9 的 `desktopTop`
  与多屏堆叠场景无法实测）、媒体键真机行为（H-7 的 keyType 32/F3 存疑项）。
- 少数低危项（L-12 的视觉结果、L-41 的 SwiftUI 窗口生命周期可达性）需要设备实测确认严重度。

---

## 7. 优先修复建议

### 立即（会造成用户数据丢失或崩溃）

1. **C-1** — 恢复带缓存目录归属校验的删除路径（回退 `a053596` 的那处替换）。
   已在 `AuditRegressionTests` 里挂好 `XCTExpectFailure`，修好后用例会提醒你删标记。
2. **C-2** — 把 `migrateClipboardTextCache` 改成按 id 重定位，去掉一次性下标快照。
3. **C-4** — `ResumeCareerTimeline.years(from:)` 加 `value < Double(Int.max)` 上界。
4. **C-3** — mosaic 绘制前反向翻转（或挪到非镜像 pass）。
5. **H-3** — 两处回滚前补 `rm -rf`；**H-2** — `resetPrivacyPermissions()` 挪到"确认可安装"之后。

### 本迭代（功能明显失效）

6. **H-7** 媒体键 F1/F2 颠倒（笔记本默认配置下两个默认快捷键都是错的）、**H-1** 壁纸筛选静默失效、
   **H-5/H-6** 下载目标冲突与残file续传；
7. **H-9** 历史截图编辑的选区错配；**H-4** 接上死掉的 TCC 清理（注意本机签名例外）；
8. **M-7** 登录项以 `SMAppService.status` 为准；**M-9/M-10** 补齐 WebView 弹窗与进程崩溃兜底；
   **M-13/M-14** 会议系统音轨的落盘顺序与 stop/start 竞态。

### 测试基建（防止同类缺陷再次漏网）

9. 给 `AppModel+Clipboard` / `AppModel+Meetings` / `AppModel+Settings` 补编排层测试。
   `AppModel.init` 已经接受 `aiAPIConnectionTester` / `meetingTranscriptionService`，
   再补 `clipboardCacheStore` / `clipboardStore` / `defaults` 注入即可，成本很低
   （本次审计正是因为无法注入这两个 store，才只能把 C-1/C-2 的复现留在 store 层与静态追踪）。
10. 在 CI 里加覆盖率下限（哪怕先只对 `AppModel*` 与 `*Store.swift` 设阈值），
    避免"291 个测试全绿 = 没问题的错觉"继续成立。
11. `ShortcutTests` 的媒体键期望值需要与 H-7 一起修正——**当前测试把错误映射固化成了断言**，
    这是"测试保护了 bug"的典型案例，值得单独复盘。

---

## 附录 A：本次交付物

| 文件 | 说明 |
| --- | --- |
| `测试报告.md` | 本报告 |
| `Tests/JarvisTests/AuditRegressionTests.swift` | 8 条回归/防回归用例（全部通过） |
| `.audit/coverage-report.txt` | `llvm-cov` 逐文件覆盖率原始数据 |

> 说明：`AuditRegressionTests.swift` 中两条针对未修复缺陷的用例使用 `XCTExpectFailure`，
> 因此套件保持全绿；修复对应缺陷后这两条会报"预期失败但没有失败"，
> 提醒维护者删除该标记并转成正常断言。
